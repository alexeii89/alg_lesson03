from hashlib import sha256

str = 'papa'
substrings = {str[i:j] for i in range(len(str)) for j in range(i + 1, len(str) + 1) if str[i:j] != str}
for i in substrings:
    print(sha256(i.encode('utf-8')).hexdigest())
